# Prevent lifecycle scripts (preinstall/install/postinstall) from running
# on npm install / npm ci, reducing supply-chain attack surface.
# The only packages with hasInstallScript in this lockfile are two copies
# of fsevents (optional macOS file-watcher), which have a JS fallback and
# are not required for the production build.
ignore-scripts=true
